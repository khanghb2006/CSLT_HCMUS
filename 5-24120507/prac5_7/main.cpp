#include<iostream>
#include<stdio.h>

#include "proc\proc.h"
// #include "proc\left\left.h"
// #include "proc\right\right.h"

using namespace std;

const int mxN = 200;

int main() {
    int n,m;
    int a[mxN][200];
    input(a,n,m);
    output(a,n,m);
}