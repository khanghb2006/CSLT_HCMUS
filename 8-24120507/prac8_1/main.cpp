#include<iostream>
#include<stdio.h>
#include<string.h>
#include<ctype.h>

#include"proc/proc.h"

using namespace std;


int main() {
    // freopen("task.inp","r",stdin);
    // freopen("task.out","w",stdout);
    char *s,*t,*r;
    input(s,t,r);
    output(s,t,r);
}