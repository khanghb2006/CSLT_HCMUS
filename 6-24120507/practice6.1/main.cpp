#include<iostream>
#include<stdio.h>
#include<ctype.h>
#include<string>
#include "proc/proc.h"

using namespace std;

int main() {
    // freopen("task.inp","r",stdin);
    // freopen("task.out","w",stdout);
    string s;
    input(s);
    output(s);
}